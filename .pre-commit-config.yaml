repos:
  - repo: local
    hooks:
      - id: npm-audit
        name: npm audit
        entry: npm audit --audit-level=low
        language: system
        always_run: true
        pass_filenames: false
        stages: [pre-commit, pre-push]
